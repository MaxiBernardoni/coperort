import type { SeasonEvent } from '@/types/event'

/**
 * TODO (Fase 3a, sin terminar): sin contenido todavía. Escribir ~5 eventos usando
 * `choice.loan` (ver `types/event.ts` / `engine/clubTransition.ts`) — el mecanismo de
 * préstamo (mover de club temporalmente y volver solo) ya está construido en el motor
 * y probado a mano, solo falta el contenido narrativo que lo dispare. Ver CLAUDE.md,
 * sección "Fase 3a".
 */
export const LOAN_EVENTS: SeasonEvent[] = []
